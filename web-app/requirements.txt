# OOVMTEL Unified View Web Application
# Python dependencies

# Web Framework
fastapi==0.108.0
uvicorn[standard]==0.25.0

# HTTP Client
httpx==0.26.0

# Data Validation
pydantic==2.5.3

# WebSocket support (included in uvicorn[standard])
websockets==12.0

# Async support
anyio==4.2.0

# JSON handling (standard library, but explicit for clarity)
# json - standard library

# Optional: For production deployment
gunicorn==21.2.0

# Optional: For environment variable management
python-dotenv==1.0.0

# =============================================
# Game-Changer Modules Dependencies
# =============================================

# For data compression in edge module
# gzip - standard library

# For statistical calculations in predictive module
# math - standard library

# For subprocess in remediation module
# subprocess - standard library

# =============================================
# LLM Integration Dependencies
# =============================================
# Note: The LLM module uses httpx (already included above) for API calls.
# This approach avoids adding heavy SDK dependencies while providing
# full functionality for Mistral, Claude, and other providers.

# Optional: Official Mistral SDK (uncomment if preferred over httpx)
# mistralai>=1.0.0

# Optional: Official Anthropic SDK for Claude (uncomment for Claude support)
# anthropic>=0.39.0

# Optional: OpenAI SDK (uncomment for OpenAI support)
# openai>=1.0.0

# Note: All game-changer modules use only standard library
# and existing dependencies (pydantic, httpx, asyncio)
